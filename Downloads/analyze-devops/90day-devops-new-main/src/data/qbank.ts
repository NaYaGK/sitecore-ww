export interface Question {
  id: string;
  cat: 'k8s' | 'gitops' | 'iac' | 'security' | 'cicd' | 'obs' | 'platform'; // category key
  diff: 'easy' | 'med' | 'hard';
  q: string;
  a: string;
}

export const QBANK: Question[] = [
  // KUBERNETES
  {id:'k1',cat:'k8s',diff:'easy',q:'What is the difference between a Pod and a Deployment in Kubernetes?',a:'A Pod is a single instance of a running process. A Deployment manages a set of identical Pods, handles rolling updates, and maintains the desired replica count. You almost always use a Deployment rather than bare Pods in production.'},
  {id:'k2',cat:'k8s',diff:'easy',q:'What does kubectl get pods -n kube-system show you?',a:'It lists all Pods in the kube-system namespace, which contains core cluster components like CoreDNS, kube-proxy, and (in managed clusters) the metrics-server. It\'s a quick way to check if the control plane add-ons are healthy.'},
  {id:'k3',cat:'k8s',diff:'med',q:'Explain the difference between ClusterIP, NodePort, and LoadBalancer service types.',a:'ClusterIP (default) is only reachable inside the cluster. NodePort exposes the service on every node\'s IP at a static port (30000-32767). LoadBalancer provisions an external cloud load balancer and is the standard way to expose services publicly in cloud environments.'},
  {id:'k4',cat:'k8s',diff:'med',q:'What is a CrashLoopBackOff and how do you debug it?',a:'CrashLoopBackOff means a container keeps crashing and Kubernetes is applying exponential backoff before restarting it. Debug with: kubectl logs <pod> --previous (see last crash logs), kubectl describe pod <pod> (see events and exit code), and check the container entrypoint and env vars.'},
  {id:'k5',cat:'k8s',diff:'hard',q:'What is the difference between a StatefulSet and a Deployment?',a:'StatefulSets give each Pod a stable, ordered identity (pod-0, pod-1…) and stable storage via volumeClaimTemplates. Pods are created/deleted in order. Deployments treat all Pods as interchangeable. Use StatefulSets for databases, distributed systems (Kafka, Zookeeper) that need stable network identity or persistent storage per replica.'},
  {id:'k6',cat:'k8s',diff:'hard',q:'Explain how Kubernetes RBAC works end-to-end.',a:'RBAC has four objects: Role (namespace-scoped rules), ClusterRole (cluster-wide), RoleBinding (binds Role to a subject in a namespace), ClusterRoleBinding. A "subject" is a User, Group, or ServiceAccount. When a request hits the API server, it checks: is this subject bound to a role that allows this verb on this resource?'},
  {id:'k7',cat:'k8s',diff:'med',q:'What is a PersistentVolume and PersistentVolumeClaim?',a:'A PersistentVolume (PV) is a piece of storage in the cluster provisioned by an admin or dynamically via a StorageClass. A PersistentVolumeClaim (PVC) is a request for storage by a user. Kubernetes binds a PVC to a matching PV. Pods mount PVCs, not PVs directly.'},
  {id:'k8',cat:'k8s',diff:'hard',q:'What happens when you run kubectl apply vs kubectl create?',a:'kubectl create is imperative — fails if the resource already exists. kubectl apply is declarative — creates if absent, patches if present using a 3-way merge between the current object, the last applied config, and the new config. Always use apply in GitOps/CI pipelines.'},

  // GITOPS
  {id:'g1',cat:'gitops',diff:'easy',q:'What are the 4 core principles of GitOps?',a:'1) Declarative: the entire system is described declaratively. 2) Versioned and immutable: desired state is stored in Git. 3) Pulled automatically: software agents pull and apply the desired state. 4) Continuously reconciled: agents continuously compare actual vs desired and correct drift.'},
  {id:'g2',cat:'gitops',diff:'easy',q:'What is the difference between ArgoCD and Flux?',a:'Both implement GitOps for Kubernetes. ArgoCD has a rich UI and is app-centric; Flux is more lightweight, CLI-driven, and toolkit-based. ArgoCD uses a push model from a central server; Flux runs as controllers inside the cluster. Flux integrates better with Helm and image update automation out of the box.'},
  {id:'g3',cat:'gitops',diff:'med',q:'What is drift detection in GitOps and why does it matter?',a:'Drift detection is when the GitOps controller compares the live cluster state against the Git-declared state and detects differences (drift). It matters because manual changes (kubectl apply by hand) create drift that makes clusters unpredictable. ArgoCD and Flux both detect drift and can auto-remediate (self-heal) by reverting to Git state.'},
  {id:'g4',cat:'gitops',diff:'med',q:'How do you manage Kubernetes secrets in a GitOps workflow without storing plaintext in Git?',a:'Options: Sealed Secrets (encrypt secrets with a cluster key, commit ciphertext to Git), External Secrets Operator (pull secrets from AWS SSM/Vault/Azure Key Vault at runtime), Mozilla SOPS (encrypt values in YAML with GPG/KMS). Sealed Secrets is the most common simple approach; External Secrets is preferred when using a secrets manager already.'},
  {id:'g5',cat:'gitops',diff:'hard',q:'Explain the app-of-apps pattern in ArgoCD.',a:'App-of-apps is a meta-Application that points to a Git directory of other ArgoCD Application manifests. The root app manages child apps. This enables managing the entire cluster configuration declaratively — when you add a new application manifest to Git, ArgoCD deploys a new Application object which then deploys the actual workload. Scales well for large clusters.'},

  // IAC / TERRAFORM
  {id:'t1',cat:'iac',diff:'easy',q:'What is idempotency in Infrastructure as Code?',a:'Idempotency means running the same IaC code multiple times produces the same result without unintended side effects. If the infrastructure already matches the declared state, nothing changes. Terraform achieves this via its plan/apply model comparing desired state against actual state stored in the state file.'},
  {id:'t2',cat:'iac',diff:'easy',q:'What is Terraform state and why is it important?',a:'Terraform state is a JSON file (terraform.tfstate) that maps your configuration to real-world resources. It tracks resource IDs, metadata, and dependencies. Without it, Terraform can\'t know what it already created. In teams, state must be stored remotely (S3, Azure Blob, Terraform Cloud) with locking to prevent concurrent apply conflicts.'},
  {id:'t3',cat:'iac',diff:'med',q:'What is the difference between terraform plan and terraform apply?',a:'terraform plan shows what changes Terraform would make (create/modify/destroy) without making them — a dry run. terraform apply executes those changes. Always review the plan output in CI before applying. In automated pipelines, use -auto-approve only in non-production environments.'},
  {id:'t4',cat:'iac',diff:'med',q:'Explain Terraform workspaces and when to use them.',a:'Workspaces let you maintain multiple state files from the same config — useful for dev/staging/prod in simple setups. However, for serious multi-environment management, separate directories or separate repos with separate state backends are preferred. Workspaces share the same code which can cause accidental cross-environment changes.'},
  {id:'t5',cat:'iac',diff:'hard',q:'What is Terraform state drift and how do you handle it?',a:'Drift occurs when real infrastructure differs from Terraform state — usually from manual changes outside Terraform. Handle it with: terraform refresh (update state to match reality), terraform import (bring unmanaged resources under Terraform control), or terraform plan -refresh=true to detect drift. Best prevention is enforcing all changes go through Terraform (via policy/RBAC).'},
  {id:'t6',cat:'iac',diff:'hard',q:'How do Terraform modules work and what makes a good module?',a:'Modules are reusable, encapsulated configurations called with input variables and returning outputs. A good module: has a single responsibility, exposes only necessary variables with sensible defaults, is versioned via a Git tag or registry, has tests (Terratest or native terraform test), and has documentation with examples. Avoid mega-modules that do too much.'},

  // SECURITY / DEVSECOPS
  {id:'s1',cat:'security',diff:'easy',q:'What is the difference between SAST and DAST?',a:'SAST (Static Application Security Testing) analyzes source code without running it — catches issues early in CI. DAST (Dynamic Application Security Testing) tests a running application by sending malicious inputs — finds runtime vulnerabilities SAST misses. Use both: SAST in every PR, DAST against staging environments.'},
  {id:'s2',cat:'security',diff:'easy',q:'What is a CVE and how do you respond to one affecting your containers?',a:'CVE (Common Vulnerabilities and Exposures) is a unique identifier for a known security vulnerability. When one affects a container: 1) Check severity (CVSS score), 2) Update the base image to a patched version, 3) Rebuild and redeploy, 4) Document the response. Automate this with Dependabot or Renovate for base image updates.'},
  {id:'s3',cat:'security',diff:'med',q:'What is OPA/Gatekeeper and how does it enforce policy in Kubernetes?',a:'OPA (Open Policy Agent) is a general-purpose policy engine. Gatekeeper is an OPA-based Kubernetes admission controller. When a resource is created/updated, Gatekeeper\'s webhook intercepts it and evaluates Rego policies (ConstraintTemplates). If it violates policy (e.g. no privileged containers), the request is rejected. Policies are stored as Kubernetes CRDs.'},
  {id:'s4',cat:'security',diff:'med',q:'What is workload identity federation and why is it better than storing secrets?',a:'Workload identity federation lets workloads (CI pipelines, pods) authenticate using short-lived OIDC tokens issued by a trusted identity provider (GitHub Actions, Kubernetes) rather than long-lived credentials. The cloud provider (Azure, AWS) trusts the OIDC issuer and exchanges the token for a cloud credential. Eliminates secret rotation, leakage risk, and key management overhead.'},
  {id:'s5',cat:'security',diff:'hard',q:'Explain the Kubernetes API server authentication and authorization flow.',a:'1) Authentication: the API server checks the request\'s credentials (client cert, bearer token, OIDC token) against configured authenticator plugins. 2) Authorization: if authenticated, RBAC checks if the subject has permission for the requested verb/resource. 3) Admission Control: admission controllers (webhooks, Gatekeeper) can mutate or reject the request. All three must pass.'},

  // CICD / PIPELINES
  {id:'c1',cat:'cicd',diff:'easy',q:'What is the difference between Continuous Integration and Continuous Deployment?',a:'CI automatically builds and tests code on every commit. CD automatically deploys tested code to production (Continuous Deployment) or to a staging environment requiring manual approval (Continuous Delivery). CD requires CI as a prerequisite. Many teams do CI + Continuous Delivery to production with a human gate.'},
  {id:'c2',cat:'cicd',diff:'med',q:'What is a GitHub Actions matrix build and when would you use it?',a:'A matrix build runs a job across multiple configurations in parallel by defining a matrix of values (e.g. node versions, OS, regions). Use it to test across multiple environments simultaneously — e.g. test on Node 18, 20, 22 in parallel rather than sequentially. Dramatically reduces CI time for multi-version or multi-platform code.'},
  {id:'c3',cat:'cicd',diff:'med',q:'How do you prevent a broken deployment from reaching production in a CI/CD pipeline?',a:'Layered gates: 1) Automated tests (unit, integration, e2e) must pass. 2) Security scans (SAST, SCA, container scan) must have no critical issues. 3) Deploy to staging first and run smoke tests. 4) Manual approval gate for production. 5) Use progressive delivery (canary/blue-green) so bad deploys only affect a percentage of traffic initially.'},
  {id:'c4',cat:'cicd',diff:'hard',q:'What is a blue-green deployment vs a canary deployment?',a:'Blue-green: two identical environments (blue=current, green=new). Switch all traffic at once from blue to green. Rollback is instant (switch back). Canary: gradually shift a percentage of traffic to the new version (5% → 25% → 100%) while monitoring metrics. Canary is safer for detecting issues in production with minimal blast radius. Blue-green is better for fast rollback.'},

  // OBSERVABILITY
  {id:'o1',cat:'obs',diff:'easy',q:'What are the three pillars of observability?',a:'Logs (discrete events with context), Metrics (numeric measurements over time), and Traces (end-to-end request flows across services). Together they answer: what happened (logs), how much/how fast (metrics), and where the time was spent (traces). OpenTelemetry is the standard for instrumenting all three.'},
  {id:'o2',cat:'obs',diff:'med',q:'What is an SLO and how do you define one?',a:'An SLO (Service Level Objective) is a target reliability goal, e.g. "99.9% of requests complete in under 200ms over a 30-day window." Define one by: 1) Identify the user journey (SLI — the metric), 2) Set a realistic target based on user happiness thresholds, 3) Calculate an error budget (100% - SLO%). Error budget governs how much risk/change you can absorb before freezing deployments.'},
  {id:'o3',cat:'obs',diff:'hard',q:'Explain distributed tracing and how context propagation works.',a:'Distributed tracing tracks a request across multiple services. Each service creates a span (start time, end time, metadata) and attaches it to a trace (a tree of spans). Context propagation passes the trace ID and parent span ID in HTTP headers (W3C TraceContext or B3) between services so they can all be linked into one trace. OpenTelemetry standardizes both instrumentation and the propagation format.'},

  // CLOUD / PLATFORM
  {id:'p1',cat:'platform',diff:'easy',q:'What is Platform Engineering and how does it differ from traditional DevOps?',a:'Platform Engineering builds Internal Developer Platforms (IDPs) — self-service tools that abstract infrastructure complexity from developers. Traditional DevOps is about culture and practices between dev and ops teams. Platform Engineering industrializes those practices into a product: golden paths, templates, and self-service APIs so dev teams move faster without needing ops expertise for every task.'},
  {id:'p2',cat:'platform',diff:'med',q:'What is FinOps and what does cloud cost optimization look like in practice?',a:'FinOps is the practice of bringing financial accountability to cloud spending. Practically: tag all resources by team/product, set up cost dashboards (AWS Cost Explorer, Kubecost), right-size underutilized resources using VPA/Goldilocks, use Spot/Preemptible instances for non-critical workloads, schedule non-prod environments to scale to zero overnight, and implement budget alerts.'},
  {id:'p3',cat:'platform',diff:'hard',q:'Design a secure CI/CD pipeline for a Kubernetes application from code push to production.',a:'1) PR triggers: SAST (CodeQL), SCA (Dependabot/Snyk), unit tests, lint. 2) Merge to main: build multi-stage Docker image, push to registry, scan image (Trivy). 3) Update image tag in GitOps repo (automated PR). 4) ArgoCD syncs to staging — run smoke tests, DAST. 5) Manual approval gate. 6) ArgoCD syncs to prod using canary rollout (Argo Rollouts). 7) Alert on error rate spike — auto-rollback trigger.'},
];
